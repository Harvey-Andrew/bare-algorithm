你是资深前端工程师/面试官。请把我给出的“前端算法题”改写成一个足够真实、贴近业务落地的场景题，并给出一个可运行的 Demo 模块的代码结构与实现（但不要给出算法的标准答案整段代码，算法只需给出接口定义、关键实现片段或伪代码/步骤说明，避免变成刷题题解）。

一、业务场景要求（必须满足）

业务背景具体：明确行业（如电商/内容/金融/IM/企业 SaaS）、角色（用户/运营/风控）、用户行为链路（进入页面→触发行为→数据变化→展示反馈）、以及触发时机（接口返回/埋点/WebSocket/滚动加载）。

数据输入输出像真实系统：给出后端接口 JSON（字段含义、类型），并包含至少 3 类“脏数据”情况：缺失字段、重复数据、乱序/延迟到达、异常值（如负数/空字符串）。

工程约束贴近现实：

数据量级：至少 10^5；

性能限制：避免主线程长任务（例如单次计算预算 16ms/50ms），必要时说明节流/分片/Worker 的策略；

内存限制：说明上限假设与数据结构选择；

需要增量更新：新数据到来时不能每次全量重算。

可观测性/可测试性：

需要设计日志与埋点字段（如 traceId、batchId、costMs、droppedCount、dirtyCount）；

提供性能指标建议（TTI、Long Task、FPS/卡顿）；

给出单元测试的边界用例清单（不少于 8 条）。

二、最终输出格式（必须按此顺序）

题目描述（业务故事 + 任务目标）

数据协议（接口 JSON、字段说明、异常情况）

约束条件（性能/复杂度/增量策略/浏览器限制）

示例输入输出（至少 2 组，其中 1 组必须是边界+脏数据）

评分点（正确性/性能/可维护性/工程化）

可选进阶挑战（并发、离线、分片计算、缓存、Worker 等）

三、Demo 模块目录结构规范（仅用于“创建 Demo 模块”时必须遵循）

必须 TypeScript（.ts/.tsx）

单文件不得超过 200 行，超出必须拆分

page.tsx 只做组合：不写业务逻辑，只导入 hooks + components

目录结构必须如下：

[module-name]/
├── page.tsx
├── types.ts
├── constants.ts
├── index.ts                # 可选
├── styles.module.css       # 可选
├── services/
│   └── [name].api.ts       # 纯函数算法 / API 封装
├── hooks/
│   └── use[Name].ts        # 状态管理 Hook
└── components/
    ├── [Feature]Graph.tsx
    ├── ControlPanel.tsx
    ├── ResultCard.tsx
    └── AlgorithmInfo.tsx


命名规范：Hook useXxx、组件 PascalCase、常量 UPPER_SNAKE_CASE、类型 PascalCase、服务 [name].api.ts

规范：函数组件用 function；文件顶部 'use client'；交互元素需要 cursor-pointer + hover + transition；services 必须纯函数无副作用。

四、输入（由我提供）
算法题，你需要严格按以上要求产出内容。

五、输出（由你提供）
请输出一份场景说明文档（PRD/技术方案简版），并补充该问题在业界/工程实践中常见的既有解决方案。要求：
场景说明文档必须包含：背景与目标、角色与链路、数据来源与时序（接口/埋点/WebSocket/滚动加载）、输入输出数据协议（JSON 字段说明+脏数据类型）、非功能约束（性能/复杂度/主线程预算/内存上限/增量更新）、失败与降级策略、可观测性指标与埋点字段、测试范围与边界用例。
既有解决方案至少给出 3 种，每种包含：适用场景、核心思路（可用伪代码/流程图文字描述）、复杂度与代价、实现要点（前端侧数据结构/调度策略/缓存策略）、常见坑与规避方式。
需要给出一个对比表（维度：正确性、性能、实现复杂度、可维护性、扩展性、对主线程影响），并说明你最终推荐的方案与理由。
不要贴完整标准答案代码；可以给关键接口定义、数据结构选择与关键步骤说明。

执行完后使用pnpm lint来检查和修复错误
