# 数据归一化 Demo

## 业务场景

Redux/状态管理中的数据 normalize - 将嵌套 API 响应转为扁平结构

## 核心算法

- 扁平化: DFS 遍历，构建 byId 映射
- 嵌套化: 构建 parentId → children 映射，再递归构建

## 算法复杂度

- 时间: O(n)
- 空间: O(n)

## 优点

- O(1) 查找任意实体
- 避免数据冗余
- 方便局部更新
